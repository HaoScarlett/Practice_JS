// 71_simplify_path

/**
 * @param {string} path
 * @return {string}
 */
var simplifyPath = function(path) {
    // spllit the input path using '/' as a delimiter
    // s = '/a/./b/../../c/' s.split('/') => ["", "a", ".", "b", "..", "..", "c", ""]
    // 
};